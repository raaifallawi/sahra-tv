/* Replace these with your real values, then save as config.js (gitignored).
   Loaded BEFORE other scripts via <script src="config.js"> in index.html
   if you want to override defaults. For GitHub Pages deployment, you can
   commit a config.js with public-safe keys (TMDB v3 read key, Supabase
   anon key) — these are designed to be public. */
window.NUJOOM_CONFIG = {
  TMDB_KEY:           "REPLACE_WITH_TMDB_V3_KEY",
  SUPABASE_URL:       "https://REPLACE.supabase.co",
  SUPABASE_ANON_KEY:  "REPLACE_WITH_ANON_KEY",
  BOT_USERNAME:       "your_bot_username",
  MINIAPP_SHORT_NAME: "app",
};
